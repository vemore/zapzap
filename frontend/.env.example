# Frontend Environment Variables

# API URL (optional)
# Only needed for development when frontend and backend run on different ports
# In production, the app will use the current origin (window.location.origin)
# Example for local development:
# VITE_API_URL=http://localhost:9999/api

# Leave empty or comment out for production build
# VITE_API_URL=
