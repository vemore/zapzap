# ZapZap Environment Variables

# Application Environment
NODE_ENV=production

# JWT Configuration (REQUIRED - generate a secure random string)
# Use: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string

# Logging Configuration
LOG_LEVEL=info

# Proxy Configuration
PROXY_PORT=80

# ====================================
# Optional Advanced Configuration
# ====================================

# Database Path (default: /app/data/zapzap.db)
# DB_PATH=/app/data/zapzap.db

# Log Directory (default: /app/logs)
# LOG_DIR=/app/logs

# Backend Port (default: 9999, don't change unless needed)
# PORT=9999

# Bot Action Delay (default: 1000ms)
# BOT_ACTION_DELAY_MS=1000

# ====================================
# AWS Bedrock Configuration (for LLM Bot)
# ====================================

# Enable LLM bot using AWS Bedrock (default: false)
# AWS_BEDROCK_ENABLED=true

# AWS Region for Bedrock (default: us-east-1)
# AWS_BEDROCK_REGION=us-east-1

# Bedrock Model ID (default: Llama 3.3 70B Instruct)
# AWS_BEDROCK_MODEL_ID=meta.llama3-3-70b-instruct-v1:0

# AWS Credentials (use IAM role in production)
# If not provided, SDK will use default credential chain:
# - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# - Shared credentials file (~/.aws/credentials)
# - IAM role (when running on AWS)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
